query GetPosts($page: Int!) {
  posts(page: $page) {
    posts {
      _id
      title
      content
      imageUrl
      createdAt
      updatedAt
      creator {
        name
      }
    }
    totalItems
  }
}
